import 'react-native';
import React from 'react';
import { BaseItemSeparator } from '../../src/components/BaseItemSeparator'; 
import {render} from '@testing-library/react-native';

it('Render component BaseHeaderTitle',()=>{
    const rendered =render(<BaseItemSeparator/>);
})